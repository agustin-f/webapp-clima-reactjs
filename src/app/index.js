import React from 'react';
import { render } from 'react-dom';

import './index.css';

render( < h1 > Esta es una app en react para ver el clima de las ciudades del mundo < /h1>,document.getElementById('app'));